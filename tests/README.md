# Tests

This folder is reserved for automated test suites as the project grows.

- Current smoke checks run via `scripts/smoke-test.mjs`.
- Future test suites can be added here (for example, route-level e2e tests).
